name: ad_trials
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - requests>=2.32
  - pandas>=2.2
  - plotly>=6
  - openpyxl>=3.1
  - pyyaml>=6
  - pycountry>=24
  - pip:
      - streamlit>=1.40,<2
      - -e .
